name: Actions

on: [push]

jobs:
    run-github:
        runs-on: ubuntu-latest
        steps:
            
            - name: simplerest
              uses: actions/checkout@v1
              
            - name: List files after
              run: |
                pwd
                ls -a
                
            - name: Setup JDK 1.8
              uses: actions/setup-java@v1
              with:
                java-version: 1.8
                          
            - name: TEst
              uses: ./testart/src/main/java/Mytest/Testyt.java@master
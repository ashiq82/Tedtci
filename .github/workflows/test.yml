name: Actions

on: [push]

jobs:
    run-github:
        runs-on: ubuntu-latest
        steps:
            
            - name: simplerest
              uses: actions/checkout@v1                                  
            - name: TEsts
              uses: ashiq82/Tedtci/testart/src/main/java/Mytest